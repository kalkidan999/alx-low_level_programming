#include "holberton.h"
/**
* rev_string -change value using the pointer address
*
*@s: stores the input
*/
void rev_string(char *s)
{
int temp;
for (i = 9; i > 0; i--)
{
    printf("%c\n", *(s + i));
}
}
