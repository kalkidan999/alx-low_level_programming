#include <stdlib.h>
#include <stdio.h>
/**
* main - lowercase and uppercase letters
* Return: success-0
*/
int main(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
letter = putchar(ch);
if (letter != e && letter != q)
{
putchar(letter);
}
return (0);
}
